spring:
  datasource:
    url: jdbc:h2:mem:chessverse;MODE=PostgreSQL;DATABASE_TO_UPPER=false
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false

