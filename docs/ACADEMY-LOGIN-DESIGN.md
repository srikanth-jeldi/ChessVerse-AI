# Academy sign-in artwork

The responsive sign-in page follows the supplied navy/gold and white split-screen reference. The original `app-icon.png` is unchanged. Unverified adoption statistics are omitted.

Background: `backend/src/main/resources/static/academy/signin-art.png`.

Generated using the built-in image generation tool with this prompt:

> Use case: stylized-concept. Asset type: production background artwork for the LEFT half of a ChessVerseAI academy login webpage. Create only the artwork, NOT a screenshot or UI. Match the user's navy/gold reference: elegant glossy black chess king with subtle blue reflections and gold edge lighting, golden orbital light curving around king, faint blue dotted globe behind, reflective navy chessboard below, a few defocused gold lights. Composition: landscape 6:5, king in RIGHT third from 35% to 85% vertical, left 58% kept mostly deep midnight navy with minimal detail to allow readable HTML heading and feature icons. No text, no letters, no logos, no numbers, no buttons, no white login card. High quality cinematic 3D product rendering; restrained luminous gold, sophisticated dark blue. Save resulting artwork as an asset.

Remember email stores only the identifier after explicit checkbox selection. Password recovery uses the existing account recovery endpoints; no credentials are stored by the new UI. Demo data remains synthetic and isolated.
